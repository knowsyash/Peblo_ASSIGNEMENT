export const metadata = {
  title: 'Login - PebloNotes',
  description: 'Sign in to your PebloNotes account.',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
