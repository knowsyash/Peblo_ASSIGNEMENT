export const metadata = {
  title: 'Sign Up - PebloNotes',
  description: 'Create a new PebloNotes account.',
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
