export const metadata = {
  title: 'Dashboard - PebloNotes',
  description: 'Your productivity dashboard and AI usage statistics.',
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
