export const metadata = {
  title: 'My Notes - PebloNotes',
  description: 'Manage and organize your personal notes.',
};

export default function NotesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
